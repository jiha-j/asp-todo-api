{
  /*
    ==========================================
    launchSettings.json - Development Launch Settings
    ==========================================

    [한글 설명]
    이 파일은 개발 환경에서 애플리케이션을 실행할 때의 설정을 정의합니다.
    Visual Studio나 Visual Studio Code에서 F5를 눌렀을 때 사용됩니다.
    Java Spring의 application-dev.properties와 유사한 역할입니다.

    [English Explanation]
    This file defines settings for running the application in development environment.
    Used when pressing F5 in Visual Studio or Visual Studio Code.
    Similar role to application-dev.properties in Java Spring.
  */

  "$schema": "http://json.schemastore.org/launchsettings.json",

  // ==========================================
  // IIS Express Profile (IIS Express 프로필)
  // ==========================================
  //
  // [한글] Visual Studio에서 IIS Express로 실행할 때 사용
  // [English] Used when running with IIS Express in Visual Studio
  "iisSettings": {
    "windowsAuthentication": false,
    "anonymousAuthentication": true,
    "iisExpress": {
      "applicationUrl": "http://localhost:5000",
      "sslPort": 5001
    }
  },

  // ==========================================
  // Launch Profiles (실행 프로필)
  // ==========================================
  //
  // [한글] 다양한 실행 방법을 정의합니다.
  //       Visual Studio에서 실행 버튼 옆 드롭다운에 표시됩니다.
  //
  // [English] Defines various launch methods.
  //           Shown in dropdown next to Run button in Visual Studio.
  "profiles": {
    // ==========================================
    // HTTP Profile - Development (개발용 HTTP 프로필)
    // ==========================================
    //
    // [한글] 기본 개발 프로필입니다.
    //       HTTP로만 실행됩니다 (HTTPS 없음).
    //       PHP 프론트엔드와 통신하기에 가장 간단합니다.
    //
    // [English] Default development profile.
    //           Runs on HTTP only (no HTTPS).
    //           Simplest for communicating with PHP frontend.
    "http": {
      "commandName": "Project",
      "dotnetRunMessages": true,
      "launchBrowser": false,  // [한글] 브라우저 자동 열기 비활성화 / Disable auto-open browser
      "applicationUrl": "http://localhost:5000",
      "environmentVariables": {
        // [한글] 개발 환경으로 설정
        // [English] Set to development environment
        "ASPNETCORE_ENVIRONMENT": "Development"
      }
    },

    // ==========================================
    // HTTPS Profile - Secure (보안 HTTPS 프로필)
    // ==========================================
    //
    // [한글] HTTPS를 사용하는 프로필입니다.
    //       SSL 인증서가 필요합니다.
    //       프로덕션 환경과 유사하게 테스트할 때 사용합니다.
    //
    // [English] Profile using HTTPS.
    //           Requires SSL certificate.
    //           Use when testing similar to production environment.
    "https": {
      "commandName": "Project",
      "dotnetRunMessages": true,
      "launchBrowser": false,
      "applicationUrl": "https://localhost:5001;http://localhost:5000",
      "environmentVariables": {
        "ASPNETCORE_ENVIRONMENT": "Development"
      }
    },

    // ==========================================
    // IIS Express Profile (IIS Express 프로필)
    // ==========================================
    //
    // [한글] Visual Studio에서 IIS Express로 실행합니다.
    //       IIS의 기능을 테스트할 때 유용합니다.
    //
    // [English] Runs with IIS Express in Visual Studio.
    //           Useful for testing IIS features.
    "IIS Express": {
      "commandName": "IISExpress",
      "launchBrowser": false,
      "environmentVariables": {
        "ASPNETCORE_ENVIRONMENT": "Development"
      }
    },

    // ==========================================
    // Swagger Profile - API Documentation (Swagger 프로필)
    // ==========================================
    //
    // [한글] Swagger UI를 자동으로 열면서 실행합니다.
    //       API 테스트와 문서화에 유용합니다.
    //
    // [English] Launches with Swagger UI automatically opened.
    //           Useful for API testing and documentation.
    "Swagger": {
      "commandName": "Project",
      "dotnetRunMessages": true,
      "launchBrowser": true,  // [한글] Swagger UI 자동 열기 / Auto-open Swagger UI
      "launchUrl": "swagger",  // [한글] Swagger 페이지로 이동 / Navigate to Swagger page
      "applicationUrl": "http://localhost:5000",
      "environmentVariables": {
        "ASPNETCORE_ENVIRONMENT": "Development"
      }
    }
  }

  // ==========================================
  // Usage in Different IDEs (IDE별 사용 방법)
  // ==========================================
  //
  // **Visual Studio 2022:**
  // - 상단 도구 모음에서 실행 프로필 선택
  // - F5: 디버깅 모드로 실행
  // - Ctrl+F5: 디버깅 없이 실행
  //
  // **Visual Studio Code:**
  // - Run and Debug (Ctrl+Shift+D)
  // - 프로필을 launch.json에 설정
  //
  // **Command Line:**
  // dotnet run --launch-profile http
  // dotnet run --launch-profile https
  //
  // **설정 우선순위 (Configuration Priority):**
  // 1. launchSettings.json (개발 환경)
  // 2. appsettings.Development.json (개발 환경)
  // 3. appsettings.json (기본값)

  // ==========================================
  // Environment Variables (환경 변수)
  // ==========================================
  //
  // [한글] ASPNETCORE_ENVIRONMENT 값:
  // - "Development": 개발 환경 (자세한 에러 메시지, Swagger 활성화)
  // - "Staging": 스테이징 환경 (프로덕션과 유사하지만 테스트용)
  // - "Production": 프로덕션 환경 (최소 로깅, 보안 강화)
  //
  // [English] ASPNETCORE_ENVIRONMENT values:
  // - "Development": Development environment (detailed errors, Swagger enabled)
  // - "Staging": Staging environment (similar to production but for testing)
  // - "Production": Production environment (minimal logging, enhanced security)

  // ==========================================
  // Application URL Configuration (애플리케이션 URL 설정)
  // ==========================================
  //
  // [한글] applicationUrl 형식:
  // - 단일 URL: "http://localhost:5000"
  // - 여러 URL: "https://localhost:5001;http://localhost:5000"
  // - 모든 네트워크 인터페이스: "http://0.0.0.0:5000"
  //
  // [English] applicationUrl formats:
  // - Single URL: "http://localhost:5000"
  // - Multiple URLs: "https://localhost:5001;http://localhost:5000"
  // - All network interfaces: "http://0.0.0.0:5000"
  //
  // 주의 (Warning):
  // 프로덕션 환경에서는 이 파일을 사용하지 않습니다!
  // This file is NOT used in production environment!

  // ==========================================
  // Troubleshooting Tips (문제 해결 팁)
  // ==========================================
  //
  // **포트가 이미 사용 중인 경우 (Port already in use):**
  // 1. applicationUrl의 포트 번호를 변경 (예: 5000 → 5555)
  // 2. frontend의 API URL도 동일하게 변경
  //
  // **HTTPS 인증서 에러 (HTTPS certificate error):**
  // dotnet dev-certs https --trust
  //
  // **환경 변수가 적용되지 않는 경우:**
  // - Visual Studio 재시작
  // - bin/ 및 obj/ 폴더 삭제 후 재빌드
}
