{
  /*
    ==========================================
    appsettings.Development.json - Development Environment Configuration
    ==========================================

    [한글 설명]
    이 파일은 개발 환경에서만 사용되는 설정 파일입니다.
    appsettings.json의 설정을 오버라이드합니다.
    개발 시 더 자세한 로그와 디버깅 정보를 표시하도록 설정합니다.

    [English Explanation]
    This file is configuration used only in development environment.
    Overrides settings in appsettings.json.
    Configured to show more detailed logs and debugging info during development.

    우선순위 (Priority):
    appsettings.Development.json > appsettings.json
  */

  // ==========================================
  // Logging Configuration (로깅 설정)
  // ==========================================
  //
  // [한글] 개발 환경에서는 더 자세한 로그를 표시합니다.
  // [English] Show more detailed logs in development environment.
  "Logging": {
    "LogLevel": {
      // [한글] 기본 로그 레벨을 Debug로 설정
      //       모든 디버그 정보를 콘솔에 표시합니다.
      // [English] Set default log level to Debug
      //           Shows all debug information in console.
      "Default": "Debug",

      // [한글] Microsoft 로그를 Information으로 설정
      //       ASP.NET Core 내부 동작을 볼 수 있습니다.
      // [English] Set Microsoft logs to Information
      //           Can see ASP.NET Core internal operations.
      "Microsoft.AspNetCore": "Information",

      // [한글] Entity Framework Core 로그를 Information으로 설정
      //       실행되는 SQL 쿼리를 콘솔에서 볼 수 있습니다.
      //       개발 시 매우 유용합니다.
      // [English] Set Entity Framework Core logs to Information
      //           Can see executed SQL queries in console.
      //           Very useful during development.
      "Microsoft.EntityFrameworkCore.Database.Command": "Information"
    }
  },

  // ==========================================
  // Development-Specific Settings
  // ==========================================
  //
  // [한글] 개발 환경 전용 설정
  // [English] Development-specific settings
  "AppSettings": {
    // [한글] 디버그 모드 활성화
    // [English] Enable debug mode
    "EnableDetailedErrors": true,

    // [한글] 민감한 데이터 로깅 (SQL 파라미터 값 표시)
    // [English] Log sensitive data (show SQL parameter values)
    "EnableSensitiveDataLogging": true
  }

  // ==========================================
  // Tips for Development (개발 팁)
  // ==========================================
  //
  // [한글] 개발 환경에서 유용한 설정:
  //       1. EnableSensitiveDataLogging: SQL 쿼리의 실제 값을 볼 수 있습니다
  //       2. DetailedErrors: 상세한 에러 정보를 클라이언트에 반환합니다
  //       3. Developer Exception Page: 자세한 예외 스택 트레이스를 표시합니다
  //
  //       주의: 프로덕션 환경에서는 절대 사용하지 마세요! (보안 위험)
  //
  // [English] Useful settings for development:
  //           1. EnableSensitiveDataLogging: Can see actual values in SQL queries
  //           2. DetailedErrors: Return detailed error info to client
  //           3. Developer Exception Page: Show detailed exception stack traces
  //
  //           Warning: Never use in production! (Security risk)
}
